# 🖼️ 照片沖印排版

純前端（HTML/CSS/JavaScript，無框架、無建置流程）的照片排版工具：先建立一份「底稿樣板」（你自己的背景設計圖 + 1～3 個照片框位置），之後就能重複套用照片、匯出成適合送洗沖印的 4×6 或 5×7 吋圖檔。跟拍立得、魔圖這類拼貼 App 的差異，是版型完全由你自己上傳、自己框位置，不受限於內建的固定樣式。

這個 repo 裡還有其他完全獨立的小工具（例如根目錄的旅遊記帳系統、[小孩獎勵紀錄本](../kids-reward-tracker/README.md)），彼此無關，只是共用同一個 repo。

## 功能

- **內建樣板包**：一開啟就有 12 款現成底稿（陽光問候、雙寶並排、大小格拼貼、拍立得三連、對話框、相簿頁、花園寫真、兩個瞬間、大圓臉、一天三景、滿版出血、雙拱門，各規劃 1～3 個照片框），標示「內建」、不能編輯/刪除，但可以直接套用匯出，方便馬上測試效果。
- **文字內容可自行編輯**：底稿上原本設計好的標題、日期、說明文字等（例如「HELLO SUNNY」「2026·08·29」「今天的天氣很好，我們去公園。」）在套用樣板時都會列成輸入欄位，改了就會即時反映在預覽跟匯出的圖檔上，不用被底稿裡的固定文字綁死。
- **自訂底稿樣板**：上傳自己的底稿圖片（例如卡片邊框、拼貼設計），在畫面上直接拖曳／縮放框出 1～3 個照片要放的位置，存成可重複使用的樣板。同一份樣板之後每次套用都是同樣的框位置。
- **樣板管理**：自訂樣板存在瀏覽器 localStorage，列表可預覽縮圖（含框位置）、編輯、刪除。
- **套用樣板匯出**：選一份樣板，依框數上傳 1～3 張照片（不必每個框都放滿），每張照片可拖曳調整位置、用滑桿縮放，即時預覽最終合成畫面。
- **沖印品質輸出**：匯出的 PNG 圖檔固定用 300 DPI 對應解析度（4×6 吋 = 1200×1800 px，5×7 吋 = 1500×2100 px），適合直接送去沖印店印出實體照片。

## 版型設計原則

- 照片框支援圓角／圓形（`radius`）、四角各自不同橢圓圓角的「不規則有機造型」（`blob`，例如花園寫真、雙拱門、滿版出血款的照片框，做法等同 CSS 的多值 `border-radius`，四角半徑加起來超過邊長時也會比照 CSS 規則等比例縮小；正橢圓、拱門、出血曲線、超出畫布邊界的滿版照片都是 `blob` 的特例，不需要額外的形狀種類）、以及旋轉角度（`rotationDeg`，例如拍立得三連款的歪斜卡片效果）。這幾個數值都跟解析度無關，可以直接套用在任何尺寸的畫布上。**但目前的自訂樣板編輯器只能拖拉出矩形、不旋轉的框**——圓角/blob/旋轉是給內建樣板包這種由設計稿產生的樣板用的，編輯器要支援手動調整可以再擴充。
- 合成順序預設是「底稿背景先畫、照片疊在框的位置上面、文字疊在最上面」，所以底稿裡照片要放的區域只要留白（不需要挖透明洞）即可，文字也不用預先烤進底稿圖裡。少數設計會有裝飾「形狀」（例如對話框的泡泡底、尾巴）疊在滿版照片「上面」——這種樣板可以額外附一張透明背景的 `foreground` 前景圖層，在照片畫完、文字畫之前疊上去（見「對話框」樣板）。
- 底稿上的文字一樣量測成跟解析度無關的資料（位置／字級比例／對齊方式／顏色／字型／旋轉角度），存在樣板的 `texts` 陣列裡，套用樣板時直接讀出對應的輸入欄位、即時用 canvas 重新畫出來，不是「找出底稿裡的文字去改」。**自訂樣板目前沒有文字欄位功能**（自己上傳的底稿本身就已經包含所有你想要的文字，不需要額外抽出來編輯）。
- 每份樣板的沖印尺寸（4×6 或 5×7）建立時就要選好；更換尺寸時底稿圖會依新的長寬比重新「鋪滿裁切」，框的相對位置不會變，但畫面比例會跟著新尺寸調整。

## 如何執行

這是純前端專案，但因為使用了 ES modules，瀏覽器對 `file://` 協定會有安全限制，**需要透過本機伺服器開啟**，不能直接雙擊 `index.html`。任選一種方式：

```bash
# 方法一：Python
python3 -m http.server 8080

# 方法二：Node.js
npx serve .
```

然後用瀏覽器開啟對應的網址（例如 `http://localhost:8080/photo-print-layout/`）。

## 部署

因為是純靜態網頁，可直接部署到 GitHub Pages、Netlify、Vercel 等靜態代管服務，不需要任何後端設定。

## 資料儲存與限制

- 內建樣板包（`builtin-templates/` 底下的圖檔＋ `js/builtin-templates.js` 的資料）是隨網站一起發布的靜態檔案，跟 localStorage 無關，清除瀏覽器資料也不會消失。
- 自訂樣板（底稿圖 + 框位置）存在瀏覽器的 `localStorage`，**只存在於目前這個瀏覽器/裝置**，換瀏覽器或清除瀏覽器資料會遺失樣板。
- 你在「套用樣板」畫面上傳的照片**不會被存起來**，只存在當下這次瀏覽器分頁的記憶體裡；離開頁面或重新整理後要重新上傳。這是刻意的設計，避免大量照片把 localStorage 的容量（通常只有 5MB 左右）塞滿。
- localStorage 有容量上限，每份樣板的底稿圖都是 300 DPI 的沖印解析度（通常幾百 KB），建議樣板數量不要無限增加；如果儲存失敗（畫面上會跳出提示），刪除不需要的舊樣板即可釋放空間。

## 專案結構

```
index.html               三個畫面的結構：樣板列表、樣板編輯器、套用樣板匯出
css/style.css             樣式
fonts/                    內建樣板包標題用的兩款字型（Gochi Hand、Nunito），自行嵌入不依賴 Google Fonts
builtin-templates/        內建樣板包的底稿圖檔（t1~t12.png，t5 額外有 t5-fg.png 前景圖層，都已經拿掉文字）
js/
  app.js                 畫面切換（gallery/editor/export）與樣板列表渲染（內建＋自訂）
  builtin-templates.js   內建樣板包的資料（框位置／圓角／旋轉角度／前景圖層／可編輯文字欄位）
  storage.js             自訂樣板的 localStorage 存取（CRUD）＋沖印尺寸的像素規格對照表
  image.js               圖片載入、底稿的「鋪滿裁切」轉換
  compositor.js          畫布合成：底稿背景 + 各框照片（含圓角/旋轉）+ 前景圖層 + 文字，預覽跟匯出共用同一套邏輯
  editor.js              自訂樣板編輯器：拖曳／縮放照片框、上傳底稿、儲存樣板
  exporter.js            套用樣板：照片上傳/拖曳/縮放、文字欄位輸入，以及最終匯出 PNG
```
